name: 试一下action功能😄
on:
    workflow_dispatch:
jobs:
    python:
        runs-on: ubuntu-latest
        steps:
            - name: python版本多少呢？
              run: python --version
            - name: 可以运行python吗🤭
              run: |
                ls
                ping www.baidu.com
                python script.py
    build:
        runs-on: windows-latest
        steps:
            - name: 我想运行一个命令
              run: |
                ls
                curl https://google.com
