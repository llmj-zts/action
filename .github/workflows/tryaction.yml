name: 试一下action功能😄
on:
    workflow_dispatch:
jobs:
    sshconnect:
        runs-on: ubuntu-latest
        steps:
            - name: Setup Debug Session
              uses: csexton/debugger-action@master
